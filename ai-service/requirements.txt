# CPU-only torch: embeddings don't need CUDA — saves ~2GB of NVIDIA wheels.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1+cpu

flask==3.0.0
# 2.2.2 imports huggingface_hub.cached_download, removed in modern huggingface_hub
# -> ImportError at startup. 2.7+ uses the current hub API.
sentence-transformers==2.7.0
huggingface_hub>=0.20,<1.0
asyncpg==0.29.0
numpy==1.26.3
